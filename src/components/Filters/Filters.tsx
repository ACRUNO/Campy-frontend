import React from "react";
import { Box } from '@mui/material'
import s from './Filters.module.css'

export default function Filters() {

    return (
        
   <Box className={s.filtersContainer} component={"div"}>

        

   </Box>
    );
}